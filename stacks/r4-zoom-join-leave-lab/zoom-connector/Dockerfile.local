FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml ./
RUN uv sync --frozen --no-install-project || uv sync --no-install-project

COPY . .

EXPOSE 8787
CMD ["uv", "run", "python", "connector.py"]
