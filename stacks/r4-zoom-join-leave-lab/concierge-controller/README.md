# Concierge Controller

Minimal Next.js operator console for bot join/leave.

## Env

Create `.env.controller` from `.env.controller.example`.

- `RUNNER_URL`: runner base URL reachable from Next.js server.

## Local Dev

```bash
cp .env.controller.example .env.controller
npm install
npm run dev
```
