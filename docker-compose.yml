services:
  bastion:
    image: ubuntu:24.04
    # Keep container alive so VS Code can attach
    command: bash -lc "sleep infinity"
    volumes:
      - .:/workspace:cached

  transport-server:
    image: livekit/livekit-server:latest
    command: ["--dev", "--bind", "0.0.0.0"]
    ports:
      - "7880:7880/tcp"
      - "7881:7881/tcp"
      - "7882:7882/udp"

  agent-runner:
    build:
      context: ./agent-runner
      dockerfile: Dockerfile.local
    volumes:
      - ./agent-runner:/app:cached
    ports:
      - "7860:7860"
    depends_on:
      - transport-server

  web-client:
    build:
      context: ./web-client
      dockerfile: Dockerfile.local
    volumes:
      - ./web-client:/web:cached
    ports:
      - "3000:3000"
    depends_on:
      - transport-server
